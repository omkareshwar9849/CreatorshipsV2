{
  "name": "creator",
  "version": "2.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "om": "cd backend && npm install && cd ../frontend && npm install",
    "omkar": "concurrently \"cd frontend && npm start\" \"cd backend && nodemon index.js\" "
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.4"
  }
}
